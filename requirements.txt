streamlit>=1.30
pandas>=2.0
requests>=2.31
ib_insync>=0.9.86
# V4 live public collectors use requests + stdlib XML/CSV parsing.
# V5.4 extended feeds: pytrends is optional; google_trends will fall back to RSS if not installed
pytrends>=4.9.0  # optional for richer Google Trends signal
#
# V6.1 NOTE — pandas-ta and yfinance were intentionally removed.
#   • pandas-ta 0.4.x requires numba+llvmlite, which doesn't build on
#     Python 3.14 (Streamlit Cloud's runtime). 0.3.x is no longer on PyPI.
#   • yfinance has frequent breakage from Yahoo's scraping countermeasures.
# Instead, app/ta_matrix.py uses Stooq's free historical CSV endpoint (same
# provider STP already uses for live quotes) and computes all 21 indicators
# hand-rolled in pandas. No third-party TA library, no scraping deps.
